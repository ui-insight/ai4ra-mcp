# Ask

Puts a question to the user and stops; the user's reply is the result. Its catalog entry declares that its output is the user's reply, which is how the pane knows to wait for it inside a workflow.

**Version:** 1.0.0 · **Output:** the user's reply

## Evals

See [`evals/`](evals/). None yet.
