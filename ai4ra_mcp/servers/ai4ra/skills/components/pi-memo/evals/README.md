# Evals

Each case lives under `cases/<case-slug>/` with `metadata.yaml` (with `validated_against_version`), `input.md` (the request and the workbook it starts from) and `expected.md` (what the output sheet must hold). None yet; the first case is `dev/examples/award-example.xlsx` with the award facts in `dev/examples/README.md`.
