# Evals

Each case lives under `cases/<case-slug>/` with `metadata.yaml` (with `validated_against_version`), `input.md` (the workbook) and `expected.md` (the six lines). None yet; the first case is `dev/examples/award-example.xlsx`.
