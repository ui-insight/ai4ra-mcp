# Evals

Each case lives under `cases/<case-slug>/` with `metadata.yaml` (with `validated_against_version`), `input.md` (the workbook) and `expected.md` (the lines). None yet; the first case is `dev/examples/current-pending-example.xlsx`.
